version: 2                            # only version at the moment 
 
requests:
  ping:
    request:
      url: http://192.168.21.199:5000/atom/ping
      method: GET
    validate:
      - jsonpath: status
        expect: 200
    log: true
