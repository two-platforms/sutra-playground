version: 2                            # only version at the moment 
 
requests:
  ping:
    request:
      url: https://api.dev.8om.tech/atom/ping
      method: GET
    validate:
      - jsonpath: status
        expect: 200
    log: true
